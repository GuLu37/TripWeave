# FastAPI Web 服务与 ASGI 启动器。
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# DeepSeek API 请求客户端。
httpx>=0.28,<1.0

# 从 backend/.env 读取并校验模型配置。
pydantic>=2.7,<3.0
pydantic-settings>=2.7,<3.0
python-dotenv>=1.0,<2.0

# 两个现有 Agent 的条件编排图。
langgraph>=1.2.9,<2.0

# LangGraph 会话状态的本地 SQLite 持久化。
langgraph-checkpoint-sqlite>=3.1,<4.0
aiosqlite>=0.22,<1.0
